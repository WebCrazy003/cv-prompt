interface:
  display_name: "Alex CV Generator"
  short_description: "Create evidence-grounded, ATS-ready CV JSON"
  default_prompt: "Use $alex-cv-generator to generate Alex’s tailored CV JSON from the runtime files."
