interface:
  display_name: "Alex CV Generator"
  short_description: "Create complete, strongly tailored ATS CV JSON"
  default_prompt: "Use $alex-cv-generator to reread the current on-disk runtime files and fully regenerate Alex’s tailored CV JSON, replacing any previous target-role content."
